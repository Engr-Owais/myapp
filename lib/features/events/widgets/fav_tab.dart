import 'package:flutter/material.dart';

class FavouriteUserRequestsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favourite User Requests Tab Content'),
    );
  }
}
